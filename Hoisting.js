// console.log(foo);  // Ref error (foo is not defined)
// foo=1;


console.log(foo2); // undefined (var variables are hoisted at the top but not intialized)
var foo2=1;

//functions also hoisted at the top

//const let are not hoisted at the top
